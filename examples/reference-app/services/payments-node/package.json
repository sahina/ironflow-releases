{
  "name": "@reference-app/payments",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The reference app's Payments context. A pull-mode Ironflow worker: sole writer of payment-{orderId} streams and the only caller of the gateway simulator.",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@ironflow/node": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "typescript": "~6.0.3",
    "vitest": "^4.1.11"
  },
  "engines": {
    "node": ">=24.2"
  }
}
