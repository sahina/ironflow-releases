{
  "name": "reference-app",
  "version": "0.1.0",
  "private": true,
  "description": "Private pnpm workspace root for the polyglot order-processing reference app. Application code lives in apps/web and services/payments-node.",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "crash:payments": "node scripts/control.mjs crash payments",
    "reset": "node scripts/reset.mjs",
    "test:contracts": "node contracts/validate.mjs",
    "test:supervisor": "node --test \"scripts/**/*.test.mjs\"",
    "test:live": "node scripts/test-live.mjs && node scripts/test-crash-resume.mjs && node scripts/test-walkthrough.mjs",
    "test:crash": "node scripts/test-crash-resume.mjs",
    "test:load": "node scripts/test-load.mjs",
    "test:walkthrough": "node scripts/test-walkthrough.mjs",
    "test": "pnpm run test:contracts && pnpm run test:supervisor && pnpm --filter \"./apps/**\" --filter \"./services/**\" --if-present test",
    "build": "pnpm --filter \"./apps/**\" --filter \"./services/**\" --if-present build",
    "lint": "pnpm --filter \"./apps/**\" --filter \"./services/**\" --if-present lint",
    "typecheck": "pnpm --filter \"./apps/**\" --filter \"./services/**\" --if-present typecheck"
  },
  "devDependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "playwright-core": "1.62.1"
  },
  "engines": {
    "node": ">=24.2"
  }
}
